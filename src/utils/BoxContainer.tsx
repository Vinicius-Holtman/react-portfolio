import { Box, styled } from "@mui/material"

export const BoxContainer = styled(Box)(({ theme }) => {
  return {
  width: "100%",
  height: "895px",

  display: "flex",
  justifyContent: "center",
  }
})